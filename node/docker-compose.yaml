services:
  frontend: 
    image: node:YOUR NODE VERSION
    restart: unless-stopped  
    container_name: app
    volumes:
      - /data:/data
    ports:
      - '3000:3000'
    build:
      context: ./
      dockerfile: Dockerfile
